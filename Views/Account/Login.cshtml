
 <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
     <div class="card shadow p-4" style="width: 350px;">
         <h2 class="text-center mb-3">Login</h2>
         <form asp-action="Login" method="post">
             <div class="mb-3">
                 <input type="email" class="form-control" name="username" placeholder="Email" required>
             </div>
             <div class="mb-3">
                 <input type="password" class="form-control" name="password" placeholder="Password" required>
             </div>
             <button class="btn btn-primary w-100">Login</button>
             <p class="text-center mt-3">
                 Don't have an account?
                 <a asp-area="" asp-controller="Account" asp-action="Register">Register</a>
             </p>

         </form>
     </div>
    
<!--
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<div class="card shadow p-4" style="width: 350px;">
    <h2 class="text-center mb-3">Login</h2>
    <form>
        <div class="mb-3">
            <input type="email" class="form-control" id="username" placeholder="Email">
        </div>
        <div class="mb-3">
            <input type="password" class="form-control" id="password" placeholder="Password">
        </div>
        <button class="btn btn-primary bthSubmit w-100">Login</button>
        <p class="text-center mt-3">
            Don't have an account?
            <a asp-area="" asp-controller="Account" asp-action="Register">Register</a>
        </p>
    </form>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js" integrity="sha512-v2CJ7UaYy4JwqLDIrZUI/4hqeoQieOmAZNXBeQyjo21dadnwR+8ZaIJVT8EE2iyI61OV8e6M8PP2/4hpQINQ/g==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

<script>
    $(document).on('click', '.bthSubmit', function () {
        debugger;
        const username = $('#username').val();
        const password = $('#password').val();

        if (!username) {
            toastr["error"]("Please enter username!!", "Error")
        }
        else if (!password) {
            toastr["error"]("Please enter password!!", "Error")

        } else {
            $.ajax({
                method: 'post',
                contentType: "application/json: charset=utf-8",
                url: '@Url.Action("Login", "Account")?username=' + username + '&password=' + password,
                success: function (res) {

                    alert("Success!");
                },
                error: function (e) { },
                beforesend: function () {
                    //$.blockui();
                },
                complete: function () {
                    //$.unblockui();
                }
            });
        }

    });
</script>

 -->


